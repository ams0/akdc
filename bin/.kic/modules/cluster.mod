###
# k3d Cluster commands
kicModule
	cluster
	k3d cluster commands

kicRunCommand
	all
	Create and bootstrap a local k3d cluster and deploy the apps
	all

kicRunCommand
	create
	Create a new local k3d cluster
	create

kicRunCommand
	delete
	Delete the k3d cluster
	delete

kicRunCommand
	jumpbox
	Deploy a 'jumpbox' to the local k3d cluster
	jumpbox

kicRunCommand
	clean
	Remove the apps from the local k3d cluster
	clean

kicRunCommand
	deploy
	Deploy the apps to the local k3d cluster
	deploy
