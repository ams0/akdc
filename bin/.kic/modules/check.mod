# description to potentially read at runtime
kicModule
	check
	Check status on the local k3d cluster

kicRunCommand
	ngsa
	Check NGSA status on the local cluster
	check-ngsa

kicRunCommand
	webv
	Check WebV status on the local cluster
	check-webv

kicRunCommand
	grafana
	Check Grafana status on the local cluster
	check-grafana

kicRunCommand
	prometheus
	Check Prometheus status on the local cluster
	check-prometheus

